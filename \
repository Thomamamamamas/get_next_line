#ifndef GET_NEXT_LINE_H
#define GET_NEXT_LINE_H
#include <stdlib.h>
#include <unistd.h>
#include <fcntl.h>

size_t			ft_strlen_mode(const char *str, int mode, int start);
char			*ft_strjoin(char const *s1, char const *s2);
char			*gnl_content(int fd, char *file);
char			*gnl_content(int fd, char *file);

#endif
